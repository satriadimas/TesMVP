<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.satriadimaspermana.tesmvp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.Pengurangan"
            android:label="@string/title_activity_pengurangan" />
        <activity
            android:name=".View.Penjumlahan"
            android:label="@string/title_activity_penjumlahan" />
        <activity
            android:name=".View.Pembagian"
            android:label="@string/title_activity_pembagian" />
        <activity
            android:name=".View.Perkalian"
            android:label="@string/title_activity_perkalian"></activity>
    </application>

</manifest>